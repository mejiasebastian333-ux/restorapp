// Roles del sistema
export const ROLES = {
  ADMIN: "admin",
  USER: "user"
};

// Estados posibles de una orden
export const ESTADOS_ORDEN = {
  PENDING: "pending",
  PREPARING: "preparing",
  DELIVERED: "delivered",
  CANCELLED: "cancelled"
};

// Impuesto (ejemplo 8%)
export const IMPUESTO = 0.08;
